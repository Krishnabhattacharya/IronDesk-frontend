enum UserType { photographer, client }

enum PrefKeys {
  userType,
  userId,
  accessToken,
  refreshToken,
  email,
  stripeAccountId,
  stripeCustomerId,
  name,
  image,
  status,
  rate,
  address,
  bio,
  service,
  equipment,
  phone,
  fcmToken,
}
